const dateAdder = require('../dailyRevenue.js')

describe('Check revenue', () => {

  it('Should be true', () => {
    const expected = true;
    const actual = true;
    expect(actual).toEqual(expected);
  });

});

