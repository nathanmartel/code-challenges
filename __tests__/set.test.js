const set = require('../e03-set/Set.js')

describe('Check Set', () => {

  it('Should be true (outputs results)', () => {
    const actual = true;
    const expected = true;
    expect(actual).toEqual(expected);
  });

});
